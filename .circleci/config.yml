version: 2.1
jobs:
  build:
    docker:
      - image: jetcipher/circleci-dotnet-core:3.0
    steps:
      - checkout
      - run:
          name: Restore
          command: dotnet restore
          working_directory: GraphLinq.Exchange
      - run:
          name: Build
          command: dotnet build -c Release
          working_directory: GraphLinq.Exchange